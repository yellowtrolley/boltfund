// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pg.tm470.boltfund.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import pg.tm470.boltfund.domain.Bolter;
import pg.tm470.boltfund.domain.BolterDataOnDemand;

privileged aspect BolterDataOnDemand_Roo_DataOnDemand {
    
    declare @type: BolterDataOnDemand: @Component;
    
    private Random BolterDataOnDemand.rnd = new SecureRandom();
    
    private List<Bolter> BolterDataOnDemand.data;
    
    public Bolter BolterDataOnDemand.getNewTransientBolter(int index) {
        Bolter obj = new Bolter();
        setEmail(obj, index);
        setFax(obj, index);
        setMobile(obj, index);
        setName(obj, index);
        setPassword(obj, index);
        setPhone(obj, index);
        setSurname(obj, index);
        setUsername(obj, index);
        return obj;
    }
    
    public void BolterDataOnDemand.setEmail(Bolter obj, int index) {
        String email = "foo" + index + "@bar.com";
        if (email.length() > 50) {
            email = email.substring(0, 50);
        }
        obj.setEmail(email);
    }
    
    public void BolterDataOnDemand.setFax(Bolter obj, int index) {
        String fax = "fax_" + index;
        if (fax.length() > 25) {
            fax = fax.substring(0, 25);
        }
        obj.setFax(fax);
    }
    
    public void BolterDataOnDemand.setMobile(Bolter obj, int index) {
        String mobile = "mobile_" + index;
        if (mobile.length() > 25) {
            mobile = mobile.substring(0, 25);
        }
        obj.setMobile(mobile);
    }
    
    public void BolterDataOnDemand.setName(Bolter obj, int index) {
        String name = "name_" + index;
        if (name.length() > 30) {
            name = name.substring(0, 30);
        }
        obj.setName(name);
    }
    
    public void BolterDataOnDemand.setPassword(Bolter obj, int index) {
        String password = "password_" + index;
        if (password.length() > 30) {
            password = password.substring(0, 30);
        }
        obj.setPassword(password);
    }
    
    public void BolterDataOnDemand.setPhone(Bolter obj, int index) {
        String phone = "phone_" + index;
        if (phone.length() > 25) {
            phone = phone.substring(0, 25);
        }
        obj.setPhone(phone);
    }
    
    public void BolterDataOnDemand.setSurname(Bolter obj, int index) {
        String surname = "surname_" + index;
        if (surname.length() > 30) {
            surname = surname.substring(0, 30);
        }
        obj.setSurname(surname);
    }
    
    public void BolterDataOnDemand.setUsername(Bolter obj, int index) {
        String username = "username_" + index;
        if (username.length() > 30) {
            username = username.substring(0, 30);
        }
        obj.setUsername(username);
    }
    
    public Bolter BolterDataOnDemand.getSpecificBolter(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Bolter obj = data.get(index);
        Long id = obj.getId();
        return Bolter.findBolter(id);
    }
    
    public Bolter BolterDataOnDemand.getRandomBolter() {
        init();
        Bolter obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Bolter.findBolter(id);
    }
    
    public boolean BolterDataOnDemand.modifyBolter(Bolter obj) {
        return false;
    }
    
    public void BolterDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Bolter.findBolterEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Bolter' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Bolter>();
        for (int i = 0; i < 10; i++) {
            Bolter obj = getNewTransientBolter(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
