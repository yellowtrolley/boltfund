// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pg.tm470.boltfund.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import pg.tm470.boltfund.domain.Eat;
import pg.tm470.boltfund.domain.EatDataOnDemand;
import pg.tm470.boltfund.domain.Location;
import pg.tm470.boltfund.domain.PriceRangeDataOnDemand;
import pg.tm470.boltfund.domain.RatingDataOnDemand;

privileged aspect EatDataOnDemand_Roo_DataOnDemand {
    
    declare @type: EatDataOnDemand: @Component;
    
    private Random EatDataOnDemand.rnd = new SecureRandom();
    
    private List<Eat> EatDataOnDemand.data;
    
    @Autowired
    PriceRangeDataOnDemand EatDataOnDemand.priceRangeDataOnDemand;
    
    @Autowired
    RatingDataOnDemand EatDataOnDemand.ratingDataOnDemand;
    
    public Eat EatDataOnDemand.getNewTransientEat(int index) {
        Eat obj = new Eat();
        setAccess(obj, index);
        setContact(obj, index);
        setEmail(obj, index);
        setFax(obj, index);
        setLink(obj, index);
        setLocation(obj, index);
        setMobile(obj, index);
        setName(obj, index);
        setNote(obj, index);
        setPhone(obj, index);
        return obj;
    }
    
    public void EatDataOnDemand.setAccess(Eat obj, int index) {
        String access = "access_" + index;
        if (access.length() > 5000) {
            access = access.substring(0, 5000);
        }
        obj.setAccess(access);
    }
    
    public void EatDataOnDemand.setContact(Eat obj, int index) {
        String contact = "contact_" + index;
        if (contact.length() > 30) {
            contact = contact.substring(0, 30);
        }
        obj.setContact(contact);
    }
    
    public void EatDataOnDemand.setEmail(Eat obj, int index) {
        String email = "foo" + index + "@bar.com";
        if (email.length() > 50) {
            email = email.substring(0, 50);
        }
        obj.setEmail(email);
    }
    
    public void EatDataOnDemand.setFax(Eat obj, int index) {
        String fax = "fax_" + index;
        if (fax.length() > 25) {
            fax = fax.substring(0, 25);
        }
        obj.setFax(fax);
    }
    
    public void EatDataOnDemand.setLink(Eat obj, int index) {
        String link = "link_" + index;
        if (link.length() > 100) {
            link = link.substring(0, 100);
        }
        obj.setLink(link);
    }
    
    public void EatDataOnDemand.setLocation(Eat obj, int index) {
        Location location = null;
        obj.setLocation(location);
    }
    
    public void EatDataOnDemand.setMobile(Eat obj, int index) {
        String mobile = "mobile_" + index;
        if (mobile.length() > 25) {
            mobile = mobile.substring(0, 25);
        }
        obj.setMobile(mobile);
    }
    
    public void EatDataOnDemand.setName(Eat obj, int index) {
        String name = "name_" + index;
        if (name.length() > 30) {
            name = name.substring(0, 30);
        }
        obj.setName(name);
    }
    
    public void EatDataOnDemand.setNote(Eat obj, int index) {
        String note = "note_" + index;
        if (note.length() > 1000) {
            note = note.substring(0, 1000);
        }
        obj.setNote(note);
    }
    
    public void EatDataOnDemand.setPhone(Eat obj, int index) {
        String phone = "phone_" + index;
        if (phone.length() > 25) {
            phone = phone.substring(0, 25);
        }
        obj.setPhone(phone);
    }
    
    public Eat EatDataOnDemand.getSpecificEat(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Eat obj = data.get(index);
        Long id = obj.getId();
        return Eat.findEat(id);
    }
    
    public Eat EatDataOnDemand.getRandomEat() {
        init();
        Eat obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Eat.findEat(id);
    }
    
    public boolean EatDataOnDemand.modifyEat(Eat obj) {
        return false;
    }
    
    public void EatDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Eat.findEatEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Eat' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Eat>();
        for (int i = 0; i < 10; i++) {
            Eat obj = getNewTransientEat(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
